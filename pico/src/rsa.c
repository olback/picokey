// #include <rsa.h>
#include <keys.h>
#include <stdint.h>
#include <tomcrypt.h>

int rsa_encrypt(uint8_t *in, size_t in_len, uint8_t *out, size_t *out_len,
                Key key) {
    return 0;
}

